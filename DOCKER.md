
## Quick Start

### Docker Compose

# Rebuild
docker compose down && docker compose up --build -d && docker compose logs -f

```bash
# Start the service
docker compose up -d

# View logs
docker compose logs -f

# Stop the service
docker compose down
```

